{{--侧边栏的菜单选项--}}
<div
        id="{{$id}}"
        data-href="{{$href}}"
        class="base_menu_sub sideMenu @if(isset($sideActive) && $sideActive === $id) active @endif"
>
    <img class="base_icon"
         @if(isset($sideActive) && $sideActive === $id)
            src="{{$iconSrcActive}}"
         @else
            src="{{$iconSrc}}"
         @endif
    />
    <span>{{$title}}</span>
</div>